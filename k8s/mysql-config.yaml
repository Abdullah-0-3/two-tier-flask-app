apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: app
data:
  MYSQL_USER: root
  init.sql: |
    CREATE TABLE messages (
    id INT AUTO_INCREMENT PRIMARY KEY,
    message TEXT
    );